# Legacy/manual manifest for local development. Prefer the Helm chart in charts/acornops-k8s-agent for release installs.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: acornops-k8s-agent
  namespace: acornops
  labels:
    app: acornops-k8s-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: acornops-k8s-agent
  template:
    metadata:
      labels:
        app: acornops-k8s-agent
    spec:
      serviceAccountName: acornops-k8s-agent
      containers:
        - name: agent
          image: acornops/agent:local
          imagePullPolicy: Never
          env:
            - name: ACORNOPS_AGENT_PLATFORM_URL
              value: "ws://host.k3d.internal:3000/agent"
            - name: ACORNOPS_CLUSTER_ID
              value: "cluster-local-456"
            - name: ACORNOPS_AGENT_KEY
              valueFrom:
                secretKeyRef:
                  name: acornops-k8s-agent-secret
                  key: agent-key
            - name: ACORNOPS_AGENT_WRITE_ENABLED
              value: "true"
            - name: ACORNOPS_AGENT_LOG_LEVEL
              value: "debug"
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
